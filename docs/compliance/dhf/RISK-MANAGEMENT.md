# 风险管理文件（Risk Management File · 骨架 v1）

> **状态**：危害情境已预填（源自仓库实证：8 项实测失败模式 + 53 条陷阱用例 + 架构红线），**风险可接受性准则与残余风险判定待管理层批准**。
> **标准依据**：GB/T 42062-2022《风险管理对医疗器械的应用》（=ISO 14971:2019）；软件应用指南 YY/T 1406.1-2016；北京局二类独立软件审评规范要求的风险管理报告结构。
> **接口**：每条危害的控制措施回指 `SRS-CN-SKILLS.md` 的 ARCH 需求与 `TRACEABILITY.md` 的验证用例——控制措施没有对应需求/用例的，视为设计缺口。

## 1. 范围

覆盖 Medcius 中国技能线全生命周期：设计开发（评测集回归）、部署（生产门闩）、使用（药师签核闭环）、上市后（审计链作为 PMS 数据源）。

## 2. 风险可接受性准则 ⬜ 待批

| 维度 | 建议刻度（示例，待批） |
|---|---|
| 严重度 S | S5 致命（直接导致严重用药伤害）；S4 危重；S3 中度；S2 轻度；S1 可忽略 |
| 概率 P | P5 频繁 … P1 极罕见（定量阈值待真实验证数据回来后校准） |
| 可接受性 | S≥4 且 P≥3 → 不可接受，必须再降险；其余按 ALARP |

## 3. 危害情境分析表

| H-ID | 危害情境 | 根因序列 | S* | 现有控制措施（→需求/用例） | 控制验证状态 | 残余风险 |
|---|---|---|---|---|---|---|
| H01 | 样例库结果被当作真实证据出具 PASS | 用户未导入官方包即跑审方 | S4 | 生产门闩（ARCH-01/09）；样例强制标注 + **硬门闩升级** `lib/production-guard.mjs` + `settlement/intake` 默认阻断 + `scripts/validate-gate.mjs` 6/6 PASS（2026-08-24） | 阻断已验证 ✅ | 待评 |
| H02 | "库中无记载"被表述为"无相互作用"，漏报真相互作用 | 语料覆盖缺口被语义翻转 | S4 | G3 三态语义纪律（ARCH-08）；`no_mention_in_corpus ≠ 无` 写死在 SKILL | 用例覆盖 ✅（rx 组） | 待评 |
| H03 | 过期说明书/编码版本导致错误比对 | 语料不随官方更新 | S4 | 版本化语料 + snapshot（ARCH-03）；输出带 effective_date | 用例覆盖 ✅（coding-04） | 待评 |
| H04 | 编造编码/凭记忆补全 | 连接器不可用时模型倾向自信答案 | S4 | 停止规则（ARCH-01/02）；抽取技能 Rule 4「永不产码」 | 用例覆盖 ✅（coding-05/-01） | 待评 |
| H05 | 断言极性翻转：否定/家族史/拟行/排除 被抽成阳性 | 模型标注错误（实测失败模式 #5） | S3 | 固定断言分类法 + worker 规则 | 用例覆盖 ✅（cne-01/02/06/07/08/10） | 待评 |
| H06 | 必填字段编造（null→捏造日期/药名） | 实测失败模式 #2 | S3 | null 默认 + completion report 计数 | 失败模式目录记录；行为用例部分覆盖 ◐ | 待评 |
| H07 | 数值单位/量纲错误通过（肌酐 mg/dL vs μmol/L） | 实测失败模式 #7 | S4 | 数值范围检查 Rule 7；标签库默认 μmol/L | 部分 ◐ | 待评 |
| H08 | 批量长文召回塌陷、静默漏抽 | 实测失败模式 #1/#6（无声错误） | S3 | 单文档单调用 + 显式完整性计数 | 结构性防护 ✅ | 待评 |
| H09 | PHI 泄漏进日志/导出/模型上下文 | 管道绕过或检测盲区 | S5 | phiguard 强制管道（ARCH-06）+ 审计库正则拒绝 + 假名盐机制 | 已知盲区：裸姓名/住址/固话/银行卡（PRIVACY-SECURITY §5）→ **升级项 R21** | 待评 |
| H10 | 审计链被删改，判定不可追溯 | 本地库被误操作/恶意修改 | S3 | append-only 触发器 + 哈希链 + verify_chain（ARCH-07） | 实现 ✅，演练待做 ⬜ | 待评 |
| H11 | 假名盐丢失 → [PSN:*] 映射永久不可解 | 环境变量管理失误 | S2 | 盐经 env 注入 + crypto.mjs 文档约定 | 文档 ✅，机构 KMS 接入待做 | 待评 |
| H12 | LLM 越权生成治疗建议文本（改变产品分类定性） | 架构漂移/提示词改动 | S5（合规维度） | 监管定位红线（ARCH-04/D1-D3），改动须过合规评审 | SKILL 成文 ✅；自动化检测 ⬜（可加 lint：输出模板字段白名单） | 待评 |
| H13 | CCHI 收费编码混入结算清单手术操作栏 | 编码体系混淆 | S3 | 体系区分规则 | 用例覆盖 ✅（coding-03） | 待评 |
| H14 | 疑似/待查诊断被赋予结算编码 | 不确定诊断处理缺陷 | S4 | 不编码规则 + 按症状编码指引 | 用例覆盖 ✅（coding-06, cne-03/10） | 待评 |

\* S 为建议初值，待准则批准后正式定级并补 P 估值。

## 4. 控制措施→需求的完整性检查

- 上表所有「用例覆盖 ✅」均可在 `TRACEABILITY.md` 定位到具体 REQ；
- **设计缺口清单**（控制存在但无自动验证）：H06/H07 行为用例不全、H09 检测盲区、H12 输出白名单 lint 缺失、H10 篡改演练未做——已并入 tracker 与 gap 分析 P1/P2；
- 新增危害的唯一入口是本表 + 同一提交更新追溯矩阵。

## 5. 上市后反馈回路（PMS 接口）

审计链事件（`rx_review_verdict`、signoff override/reject、verify_chain 异常）即上市后监督的数据源：

1. 药师 `override/reject` 比率异常上升 → 触发规则缺陷归因流程；
2. `validation_batch` 事件绑定 gold 文件哈希 → 每次回顾性验证自动进入 PMS 记录；
3. 危害 H02/H03 的现场发生率从审计链直接统计，替代主观估计。

## 6. 待决事项

- ⬜ 准则批准（§2）后回填 S/P 定级与残余风险结论
- ⬜ 风险管理报告的申报格式排版（按 121 号公告附件格式另出正式版）
